#include <iostream>

int main() {
    std::cout << "[Netife Dispatcher] Netife is running... ..." << std::endl;
    std::cout << "[Netife Dispatcher] Netife Dispatch is not a individual component, which need "
                 "not only NetifeProbe but also NetifeCore." << std::endl;

    return 0;
}